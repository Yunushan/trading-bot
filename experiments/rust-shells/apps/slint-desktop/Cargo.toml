[package]
name = "trading-bot-slint-desktop"
version = "0.1.0"
edition = "2024"
publish = false
build = "build.rs"

[build-dependencies]
slint-build.workspace = true

[dependencies]
slint.workspace = true
trading-bot-core = { path = "../../crates/core" }
