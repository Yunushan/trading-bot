"""
Backward-compatible import shim for core strategy position-flip helpers.

New code should import from ``app.core.strategy.strategy_position_flip_runtime``.
"""

from app.core.strategy.strategy_position_flip_runtime import *
