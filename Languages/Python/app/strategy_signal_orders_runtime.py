"""
Backward-compatible import shim for the strategy signal-order runtime binder.

New code should import from ``app.core.strategy.strategy_signal_orders_runtime``.
"""

from app.core.strategy.strategy_signal_orders_runtime import *

