"""
Backward-compatible import shim for core strategy position-close helpers.

New code should import from ``app.core.strategy.strategy_position_close_runtime``.
"""

from app.core.strategy.strategy_position_close_runtime import *

