"""
Backward-compatible import shim for core strategy runtime helpers.

New code should import from ``app.core.strategy.runtime.strategy_runtime``.
"""

from app.core.strategy.runtime.strategy_runtime import *

